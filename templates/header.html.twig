{%  block header %} 
    <header>

        <a href="{{ path('app_home') }}"> <img src="{{ asset('images/Mdl.png') }}" alt="Logo MDL" id="logo-mdl"></a>
        <nav>
            <ul>

                {% if is_granted('IS_AUTHENTICATED_FULLY') %}
                    <li><a href="{{ path('app_inscription') }}">Inscription</a></li>
                    <li><a href="{{ path("app_logout") }}"><i class="fa fa-sign-out" aria-hidden="true"></a></i></li>
                    {% else %}
                    <li><a href="{{ path('app_register') }}">Créer mon compte</a></li>
                    <li><a href="{{ path('app_login') }}">Connexion</a></li>
					 <li><a href="{{ path('app_ajouterateliers') }}">Ajouter un Activitée</a></li>
					<li><a href="{{ path('app_atelier') }}">Modifier un Vacation </a></li>
                    {% endif %}

            </ul>
        </nav>
    </header>

{%  endblock %}